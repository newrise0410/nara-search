# nara-search — NARA SEARCH 확장·개선 계획

레퍼런스: https://nara-search.vercel.app (app.js v1.9.1 / API 0.3.44)

## 1. 레퍼런스 현황 (실측)

| 워크스페이스 | 기능 |
|---|---|
| 통합검색 | 낙찰결과·입찰공고·사전규격 3종 / 기간 프리셋(1개월~5년) / 빠른·정밀 조회 / 조건 프리셋 저장·링크 복사 / 최근검색어 12개 |
| 결과 | 결과 내 검색, 유형 필터, 정렬(금액·마감·최신), 밀도 토글, CSV·Excel·복사·공유·제안서·공고 내보내기, 투찰업체 조회, 30건씩 더보기 |
| 평가분석 | 기술평가 점수: 분석 브리핑(1위 대비), 평가위원 합계, 분야별 배점 |
| 관심 키워드 | 최대 30개, 앞 8개 일괄 점검 |
| 경쟁사 | 사업자번호/업체명 → 기간별 낙찰 건수·금액 집계 |
| 저장내역 | 브라우저 다운로드 이력 표시 |
| 알림·설정 | 카카오 나에게 보내기, 5/15/30분 점검, 예약알림(입찰 예약·낙찰 감시), 백업 내보내기/가져오기, 다크·라이트 |

백엔드 엔드포인트(추정 Vercel serverless): `/api/search, pre-spec, award-bidders, notice-detail, pre-spec-detail, attachment-bundle, technical-scores, export-xlsx, competitor-lookup, competitor-analysis, keyword-check, kakao-*, reservations*, diagnostics, health`

구조 특징: 단일 `app.js`(234KB) 바닐라 SPA, 해시 라우팅, localStorage 상태, 서비스워커, 서버 DB 없음(예약만 서버).

## 2. 레퍼런스의 한계 (개선점)

1. **상태가 브라우저에 갇힘** — 키워드·경쟁사·프리셋이 localStorage. 기기 바꾸면 소실, 팀 공유 불가.
2. **검색이 "조회"에 머묾** — 매번 나라장터 API를 실시간 호출. 히스토리/추세 분석 불가, 정밀 조회가 느림.
3. **조달 도메인 특화 없음** — 범용 조달 검색. 조달 사업자에게 필요한 분류(장비/서비스/교육/방제/측량/점검)·인증요건(참가자격, 실적요건)·공고 유형 구분이 없음.
4. **알림 채널이 카카오 1개** + 나에게 보내기 한정(팀 공유 불가).
5. **평가분석이 사후 분석만** — 낙찰가율·투찰 분포 예측, 예정가격 대비 낙찰률 통계 없음.
6. **모놀리식 234KB JS** — 테스트·유지보수·협업 어려움. 접근성/모바일 대응은 있으나 컴포넌트 재사용 불가.
7. **저장내역이 실제 저장소가 아님** — 다운로드 로그일 뿐.
8. **키워드 점검 8개 제한**, 경쟁사 분석도 요청 시점 집계 → 배치/캐시 부재.

## 3. 확장 방안

### A. 데이터 레이어 (핵심 차별화)
- **수집 파이프라인**: 나라장터 OpenAPI(조달청 공공데이터) + 사전규격/낙찰 결과를 주기 크롤링 → Postgres(Supabase) 적재. 수년치 히스토리 확보.
- 전문검색(pg_trgm / Meilisearch) → 즉시 응답, 오프라인 API 장애에도 서비스 유지.
- 정규화 테이블: notices, prespecs, awards, bidders, agencies, companies, tech_scores.

### B. 사용자 커스텀 도메인 프로필 (구현됨 — web/)
- 특화 주제를 하드코딩하지 않고 **프로필**로 일반화: 분류 카테고리(포함/제외 단어, 색), 요건 키워드, 기본 검색어를 사용자가 직접 정의.
- 활성 프로필 규칙으로 결과 자동 태깅 → 분류별 필터·집계. "조달 사업" 프로필은 기본 예시일 뿐, 삭제/교체 가능.
- 다음 단계: 규칙 기반 → LLM 보조 분류(프로필 설명을 프롬프트로), 공고 상세에서 요건 추출, 프로필별 대시보드, 프로필 공유(JSON 내보내기는 이미 지원).

### C. 인텔리전스
- **낙찰 예측/투찰 가이드**: 동일 기관·유사 공고의 낙찰률 분포, 예정가격 대비 낙찰가 히스토그램, 추천 투찰 구간.
- **경쟁사 프로파일**: 업체별 낙찰 이력·주력 분야·주요 거래기관·평균 낙찰률 페이지(정적 URL로 공유 가능).
- **공고 유사도**: 과거 유사 공고와 그 결과 연결("이 공고와 비슷한 과거 사업").
- 사전규격 → 본공고 전환 추적(예고 후 실제 공고 게시 시 알림).

### D. 협업·계정
- Supabase Auth 로그인, 팀 워크스페이스. 키워드·경쟁사·프리셋을 서버 저장하고 팀 공유.
- 공고별 메모·담당자·상태(검토/참여/불참/제출) 칸반 — 간단한 입찰 CRM.
- 파일 저장소: 첨부파일 번들·제안서 초안을 실제 스토리지에 보관.

### E. 알림 확장
- 카카오 + 이메일 + Slack/Discord 웹훅 + 웹푸시. 팀 채널 발송.
- 조건형 알림: "예산 ≥ N원 AND 지역=경기 AND 태그=측량". 일일 다이제스트 모드.

### F. 문서 생성
- 제안서 초안: 레퍼런스의 "제안서" 버튼을 확장 → 공고 요건 + 회사 프로필 + 과거 실적으로 LLM 초안(docx).
- 입찰 체크리스트 자동 생성(제출 서류·마감·설명회 일정 → 캘린더 ics).

## 4. 기술 스택 제안

- Frontend(현재): Vite + React 19 + TypeScript + zustand(persist) + react-router(Hash). 서버 기능(알림·수집)이 필요해지면 Next.js로 전환 또는 별도 API 서버 추가. 레퍼런스의 다크 테마·7개 워크스페이스 IA는 유지.
- Backend: Next.js Route Handlers + Supabase(Postgres, Auth, Storage, Cron). 수집 워커는 Vercel Cron 또는 별도 Node 워커.
- 검색: Postgres FTS(초기) → Meilisearch(규모 커지면).
- LLM: Claude API(분류·요건 추출·제안서 초안), 배치 처리로 비용 통제.
- 테스트: Vitest + Playwright. CI: GitHub Actions.

## 5. 단계별 로드맵

| 단계 | 기간(안) | 산출물 |
|---|---|---|
| 0. 부트스트랩 ✅ | 완료 | `web/` Vite+React+TS 스캐폴드, 6개 워크스페이스, 커스텀 프로필, 데모 모드 |
| 0.5 API 어댑터 ✅ | 완료 | 사전규격(v1.0)·개방표준(v1.2: 공고/낙찰/계약) 문서 기준 구현, 청킹·병렬·캐시·진행률, 투찰업체 그룹화 |
| 1. 패리티 | 2주 | 3종 조회, 내보내기, 키워드·경쟁사, 카카오 알림 — 레퍼런스 동등 기능 |
| 2. 데이터 레이어 | 2주 | 수집 파이프라인 + DB, 히스토리 검색, 응답속도 개선 |
| 3. 조달 특화 | 2주 | 자동 태깅, 요건 추출, 조달 대시보드 |
| 4. 협업·알림 | 2주 | 로그인, 팀 공유, 입찰 CRM, 멀티채널 알림 |
| 5. 인텔리전스 | 3주 | 낙찰률 통계, 투찰 가이드, 경쟁사 프로파일, 제안서 초안 |

## 6. 우선순위 판단

가장 큰 레버리지는 **A(데이터 레이어)** 와 **B(조달 특화)**. 레퍼런스는 이미 "잘 만든 조회 도구"이므로 같은 걸 다시 만드는 것보다, 히스토리 데이터 + 도메인 태깅으로 "조달 사업자를 위한 조달 인텔리전스"로 포지셔닝하는 것이 확장의 핵심.

## 6.5 API 제약이 로드맵에 주는 시사점
- 낙찰 API는 개찰일 **1일** 범위만 허용 → 장기간 조회는 브라우저에서 수백 회 호출. **A(데이터 레이어) 우선순위가 더 올라감**: 서버가 매일 1회 전일 낙찰을 수집해 두면 클라이언트는 DB만 조회.
- 공고/낙찰/계약 API에 키워드 파라미터가 없음 → 서버 수집 + 자체 검색 인덱스가 사실상 필수.
- 낙찰 응답이 투찰업체 단위라 투찰 분포·낙찰률 통계(C 인텔리전스)는 추가 API 없이 계산 가능.

## 7. 확인 필요 사항
- 나라장터 OpenAPI 키 보유 여부 / 호출 한도
- 카카오 개발자 앱(레퍼런스와 동일 방식 사용 여부)
- 단일 사용자용 vs 팀/상용 서비스 목표 (D 단계 규모 결정)
- 레퍼런스 소스 접근 가능 여부(같은 작성자라면 백엔드 재사용 가능)
